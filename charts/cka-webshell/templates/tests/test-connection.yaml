apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cka-webshell.fullname" . }}-test-connection"
  labels:
    {{- include "cka-webshell.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cka-webshell.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
