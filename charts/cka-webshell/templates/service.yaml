apiVersion: v1
kind: Service
metadata:
  name: {{ include "cka-webshell.fullname" . }}
  labels:
    {{- include "cka-webshell.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cka-webshell.selectorLabels" . | nindent 4 }}
